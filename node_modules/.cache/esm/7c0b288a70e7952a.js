let CustomErrorHandler,JwtService;_70e‍.x([["default",()=>_70e‍.o]]);_70e‍.w("../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_70e‍.w("../services/JwtService",[["default",["JwtService"],function(v){JwtService=v}]]);


const auth = async ( req, res, next ) => {
    let authHeader = req.headers.authorization;
    // console.log(authHeader);
    
    // header nahi mil raha tab 
    if(!authHeader) {
        return next(CustomErrorHandler.unAuthorized());
    }

    const token = authHeader.split(' ')[1];
    // console.log("__TOKEN___ ",token);

    // VARIFY THE TOKEN
    try {
        const {_id, role} = await JwtService.verify(token);
        
        const user = {
            _id: _id,
            role: role
        };

        req.user = user;
        next();
        
    } catch (err) {
        return next(CustomErrorHandler.unAuthorized());
    }
};

_70e‍.d(auth);