let jwt,JWT_SECRET;_e33‍.x([["default",()=>_e33‍.o]]);_e33‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);_e33‍.w("../config",[["JWT_SECRET",["JWT_SECRET"],function(v){JWT_SECRET=v}]]);


class JwtService {
    // 1.) sign the token
    static sign(payload, expiry='60s', secret=JWT_SECRET){
        return jwt.sign(payload, secret, {expiresIn: expiry});
    }

    // 2.) varify the token
    static verify(token, secret=JWT_SECRET) {
        return jwt.verify(token, secret);
    }
}

_e33‍.d(JwtService);