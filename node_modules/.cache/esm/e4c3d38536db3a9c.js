let Joi,RefreshTokens,User,CustomErrorHandler,bcrypt,JwtService,REFRESH_SECRET;_36d‍.x([["default",()=>_36d‍.o]]);_36d‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_36d‍.w("../../models",[["RefreshTokens",["RefreshTokens"],function(v){RefreshTokens=v}],["User",["User"],function(v){User=v}]]);_36d‍.w("../../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_36d‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_36d‍.w("../../services/JwtService",[["default",["JwtService"],function(v){JwtService=v}]]);_36d‍.w("../../config",[["REFRESH_SECRET",["REFRESH_SECRET"],function(v){REFRESH_SECRET=v}]]);






const loginController = {
   // Login method 
   async login(req, res, next){
       //login 

       // 1.) CREATE LOGIN SCHEMA
       const loginSchema = Joi.object({
            email: Joi.string().email().required(),
            password: Joi.string().pattern(new RegExp('^[a-zA-Z0-9]{3,30}$')).required()
       });

       // 2.) VALIDATE SCHEMA 
       const { error } = loginSchema.validate(req.body);

       if(error) {
        return next(error);
       }

       // 3.) CHECK EMAIL AND PASSWORD 
       try {
           const user = await User.findOne({email: req.body.email})
            if(!user) {
                return next(CustomErrorHandler.wrongCredentials());
            }

            // compare/check password 
             const match = await bcrypt.compare(req.body.password, user.password);
             if(!match) {
                return next(CustomErrorHandler.wrongCredentials());
             }

             // generate token
             const access_token = JwtService.sign({_id: user._id, role: user.role});
             const refresh_token = JwtService.sign({_id: user._id, role: user.role}, '1y', REFRESH_SECRET);

             //database whitelist
             await RefreshTokens.create({
               token: refresh_token
             });
             
             // send token in as a response
             res.json({
                access_token: access_token,
                refresh_token: refresh_token
             });


       } catch (err) {
        return next(err);
       }
       
        
    },


   //  logout method
   async logout(req, res, next) {
      // logout logic
      // create schema
      const logoutSchema = Joi.object({
         refresh_token: Joi.string().required()
      });

      // validate 
      const {error} = logoutSchema.validate(req.body);

      if(error) {
         return next(error);
      }

      try {
         await RefreshTokens.deleteOne({token: req.body.refresh_token});
         res.json({
            status: 1
         });
      } catch (err) {
         return next(new Error('Something went wrong in the database.'));
      }
   }

};

_36d‍.d(loginController);