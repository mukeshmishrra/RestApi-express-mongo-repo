let User,CustomErrorHandler;_81d‍.x([["default",()=>_81d‍.o]]);_81d‍.w("../../models",[["User",["User"],function(v){User=v}]]);_81d‍.w("../../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);


const userController = {
   async me(req, res, next) {
        try {
            //select use krk unwanted data jo hame nahi dikhane user ko so wo hum yha remove kr skte hai select ka use krk.
            const user = await User.findOne({ _id: req.user._id }).select('-password -updatedAt -__v');
            
            // if there is no user
            if(!user) {
                return next(CustomErrorHandler.notFound());
            } 

            // if there is user
            res.json(user);

        } catch (err) {
            return next(err);
        }
    }
};

_81d‍.d(userController);